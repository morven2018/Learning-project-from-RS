export enum CssTags {
  Main = 'main',
  Header = 'header',
  Section = 'section',
  Button = 'button',
  Form = 'form',
  Input = 'input',
  Div = 'div',
}

export enum CssClasses {
  Main = 'root',
  NotFound = 'not-found',
  Login = 'login-form',
  Chat = 'chat-page',
  About = 'about-page',
}
