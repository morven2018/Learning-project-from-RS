export enum CssClasses {
  Header = 'header',
  Main = 'main',
  Garage = 'garage',
  Winners = 'winners',
  NotFound = 'not-found',
  ToGarage = 'btn-to-garage',
  ToWinners = 'btn-to-winners',
  Disable = 'disable',
}

export enum CssTags {
  Main = 'main',
  Header = 'header',
  Section = 'section',
  Button = 'button',
}

export enum Pages {
  Garage = 'garage',
  Winners = 'winners',
  Notfound = 'not-found',
}
