import { CssClasses, CssTags } from './enums';

export const notFoundParameters = {
  tag: CssTags.Section,
  classNames: [CssClasses.NotFound],
};

export const aboutParameters = {
  tag: CssTags.Section,
  classNames: [CssClasses.About],
};

export const loginParameters = {
  tag: CssTags.Form,
  classNames: [CssClasses.Login],
};

export const chatParameters = {
  tag: CssTags.Section,
  classNames: [CssClasses.Chat],
};
